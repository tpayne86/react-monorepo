- type: serial
  name: healthifyme-web-frontend
  steps:
  - name: unit-tests
    service: healthifyme
    command: export NODE_ENV=test&&yarn app:test

  - name: app-build
    service: healthifyme
    command: export NODE_ENV=production&&yarn app:build
