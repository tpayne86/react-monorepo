{
  "private": true,
  "name": "@healthifyme/utilities",
  "version": "4.0.2",
  "description": "all utility functions, services that can be shared accross the project should be kept here.",
  "main": "lib/index.js",
  "scripts": {
    "watch": "yarn clean && yarn transpile --source-maps inline --watch",
    "build": "yarn clean && yarn env:prod && yarn transpile",
    "clean": "rm -rf ./lib",
    "transpile": "babel src --out-dir lib --copy-files --ignore spec.js,spec.jsx",
    "env:prod": "export NODE_ENV=production"
  },
  "repository": {
    "type": "git",
    "url": "none"
  },
  "keywords": [
    "jest",
    "utilities",
    "services",
    "javascript"
  ],
  "author": "hannad <hannad63@yahoo.com>",
  "license": "MIT",
  "dependencies": {
    "@healthifyme/constants": "^4.0.2"
  },
  "devDependencies": {
    "@babel/cli": "7.2.0",
    "eslint": "^5.10.0"
  },
  "peerDependencies": {
    "axios": "^0.18.0"
  }
}
