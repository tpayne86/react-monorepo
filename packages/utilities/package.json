{
  "private": true,
  "name": "@healthifyme/utilities",
  "version": "1.1.3",
  "description": "all utility functions, services that can be shared accross the project should be kept here.",
  "main": "index.js",
  "scripts": {
    "watch": "yarn clean && yarn transpile --source-maps inline --watch",
    "build": "yarn clean && yarn env:prod && yarn transpile",
    "clean": "rm -rf ./dist",
    "transpile": "babel src --out-dir dist --copy-files --ignore spec.js,spec.jsx",
    "env:prod": "export NODE_ENV=production"
  },
  "repository": {
    "type": "git",
    "url": "none"
  },
  "keywords": [
    "jest",
    "utilities",
    "services",
    "javascript"
  ],
  "author": "hannad <hannad63@yahoo.com>",
  "license": "MIT",
  "dependencies": {
    "@healthifyme/components": "^1.1.3",
    "@healthifyme/constants": "^1.1.2",
    "@healthifyme/elements": "^1.1.3",
    "@healthifyme/test": "^1.2.0"
  },
  "devDependencies": {
    "@babel/cli": "7.0.0",
    "eslint": "^5.5.0",
    "jest": "^23.5.0",
    "jest-cli": "^23.5.0"
  }
}
