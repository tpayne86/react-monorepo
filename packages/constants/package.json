{
  "private": true,
  "name": "@healthifyme/constants",
  "version": "4.0.1",
  "description": "core constants for through out the application",
  "main": "lib/",
  "scripts": {
    "watch": "yarn clean && yarn transpile --source-maps inline --watch",
    "build": "yarn clean && yarn transpile",
    "clean": "rm -rf ./lib",
    "transpile": "babel src --out-dir lib --copy-files --ignore spec.js,spec.jsx"
  },
  "devDependencies": {
    "@babel/cli": "7.1.5"
  },
  "keywords": [
    "javascript",
    "constants",
    "healthifyme"
  ],
  "author": "hannad <hannad63@yahoo.com>",
  "license": "MIT"
}
