{
  "private": true,
  "name": "@healthifyme/services",
  "version": "1.0.1",
  "description": "this package will hold are service/helper functions that can be re-used accross the application",
  "main": "/dist/index.js",
  "scripts": {
    "watch": "yarn clean && yarn transpile --source-maps inline --watch",
    "build": "yarn clean && yarn env:prod && yarn transpile",
    "clean": "rm -rf ./dist",
    "transpile": "babel src --out-dir dist --copy-files --ignore spec.js,spec.jsx",
    "env:prod": "export NODE_ENV=production"
  },
  "dependencies": {
    "@healthifyme/constants": "^2.0.1"
  },
  "devDependencies": {
    "@babel/cli": "7.0.0"
  },
  "keywords": [],
  "author": "hannad <hannad63@yahoo.com>",
  "license": "MIT"
}
