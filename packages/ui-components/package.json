{
  "private": true,
  "name": "@healthifyme/ui-components",
  "version": "4.0.4",
  "description": "a repository for react common components. which will be elements like button,modal,paginations etc and modals like table,slider,layaout etc",
  "main": "lib/index.js",
  "author": "hannad rehman",
  "license": "MIT",
  "scripts": {
    "watch": "yarn clean && yarn transpile --source-maps inline --watch",
    "build": "yarn clean && cross-env NODE_ENV=production yarn transpile",
    "clean": "rm -rf ./lib",
    "transpile": "babel src --out-dir lib --copy-files --ignore spec.js,spec.jsx"
  },
  "dependencies": {
    "@healthifyme/constants": "^4.0.4",
    "@healthifyme/styles": "^3.0.2"
  },
  "peerDependencies": {
    "antd": "^3.9.0",
    "prop-types": "^15.6.2",
    "react": "^16.4.2",
    "react-router-dom": "^4.3.1",
    "styled-components": "*"
  },
  "devDependencies": {
    "@babel/cli": "7.2.3"
  }
}
